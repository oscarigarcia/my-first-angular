import { ConvertToSpacePipe } from './convert-to-space.pipe';

describe('ConvertToSpacePipesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
